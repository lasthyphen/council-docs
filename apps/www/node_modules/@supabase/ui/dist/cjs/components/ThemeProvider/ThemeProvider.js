'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var defaultTheme = require('../../lib/theme/defaultTheme.js');
var mergeDeep = require('../../utils/mergeDeep.js');

const ThemeContext = /*#__PURE__*/React.createContext({
  theme: defaultTheme['default'] // mode: 'light',
  // toggleMode: true,

});

const ThemeProvider = ({
  children,
  theme: customTheme // dark,
  // usePreferences = false,

}) => {
  const mergedTheme = mergeDeep.mergeDeep(defaultTheme['default'], customTheme); // const [mode, setMode, toggleMode] = useDarkMode(usePreferences)
  // useLayoutEffect(() => {
  //   if (dark) {
  //     if (setMode != null) {
  //       setMode('dark')
  //     }
  //     document.documentElement.classList.add(`dark`)
  //   }
  // }, [dark])

  const value = React.useMemo(() => ({
    theme: mergedTheme // mode,
    // toggleMode,

  }), []); // console.log('defaultTheme', defaultTheme)

  return jsxRuntime.jsx(ThemeContext.Provider, Object.assign({
    value: value
  }, {
    children: children
  }), void 0);
};

exports.ThemeContext = ThemeContext;
exports['default'] = ThemeProvider;
