import React from 'react';
interface Props {
    variant?: 'success' | 'danger' | 'warning' | 'info' | 'neutral';
    className?: string;
    title: string;
    withIcon?: boolean;
    closable?: boolean;
    children?: React.ReactNode;
    icon?: React.ReactNode;
    actions?: React.ReactNode;
}
declare function Alert({ variant, className, title, withIcon, closable, children, icon, actions, }: Props): JSX.Element;
export default Alert;
